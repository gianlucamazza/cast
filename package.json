{
  "name": "cast-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox/LibreWolf extension that casts the current tab or mirrors the browser window to a Chromecast via the local skill-cast helpers.",
  "scripts": {
    "lint": "web-ext lint -c web-ext-config.cjs",
    "start": "web-ext run -c web-ext-config.cjs",
    "build": "web-ext build -c web-ext-config.cjs"
  },
  "devDependencies": {
    "web-ext": "^8.3.0"
  }
}
